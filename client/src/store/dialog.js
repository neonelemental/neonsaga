const state = {
  dialogMessages: [
    "This is a message, welcome to the game!  It's wonderful to have you...",
    "To continue, select a character and give it a name, then we can start playing."
  ]
};

const actions = {};
const getters = {};
const mutations = {};

export default {
  namespaced: true,
  state,
  actions,
  getters,
  mutations,
};